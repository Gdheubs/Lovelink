{
  "name": "loverlink",
  "version": "0.1.0",
  "private": true,
  "description": "Loverlink — voice-first digital third place. Dependency-free core, ports & adapters.",
  "workspaces": [
    "apps/server",
    "apps/web"
  ],
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "dev:memory": "npm run dev:memory --workspace @loverlink/server",
    "dev": "npm run dev --workspace @loverlink/server",
    "dev:web": "npm run dev --workspace @loverlink/web",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspace @loverlink/server",
    "test:unit": "npm run test:unit --workspace @loverlink/server",
    "test:integration": "npm run test:integration --workspace @loverlink/server",
    "migrate": "npm run migrate --workspace @loverlink/server",
    "smoke": "npm run smoke --workspace @loverlink/server",
    "lint": "eslint apps/server/src apps/server/tests",
    "format": "prettier --write \"apps/**/*.{ts,tsx,json,md}\" \"docs/**/*.md\" \"*.md\"",
    "format:check": "prettier --check \"apps/**/*.{ts,tsx,json,md}\" \"docs/**/*.md\" \"*.md\"",
    "ci": "npm run format:check && npm run lint && npm run typecheck && npm run test:unit",
    "compose:up": "docker compose up -d",
    "compose:down": "docker compose down"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1"
  },
  "type": "module"
}
